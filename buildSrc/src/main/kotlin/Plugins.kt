object Plugin {

    const val android_gradle =
        "com.android.tools.build:gradle:8.4.0"

    const val google_services =
        "com.google.gms:google-services:${Version.google_services}"

    const val crashlytics =
        "com.google.firebase:firebase-crashlytics-gradle:${Version.crashlytics}"

    const val kotlin =
        "org.jetbrains.kotlin:kotlin-gradle-plugin:${Version.kotlin}"

    const val kotlinter =
        "org.jmailen.gradle:kotlinter-gradle:${Version.kotlinter}"

    const val safeArgs =
        "androidx.navigation:navigation-safe-args-gradle-plugin:${Version.navigation}"

    const val exhaustive =
        "app.cash.exhaustive:exhaustive-gradle:${Version.exhaustive}"

}